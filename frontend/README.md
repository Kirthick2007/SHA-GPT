# ClaimShield AI Frontend

This is the React dashboard for ClaimShield AI.

## Step 4: Run The Frontend

Keep the backend running first:

```text
http://127.0.0.1:8000
```

Then open a second terminal in the `frontend` folder and run:

```bash
npm install
npm run dev
```

Open:

```text
http://127.0.0.1:5173
```

